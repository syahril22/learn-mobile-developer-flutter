void main() {
  // type data dynamic valuenya bisa berubah ubah type data, atau kita juga bisa buat variabel tanpa dideklarasikan maka variabel itu secara otomatis menjadi type data dynamic

  // example variabel don't declaration
  var value;
  print(value);

  dynamic variabel = 100;
  print(variabel);

  variabel = "syahril";
  print(variabel);

  variabel = true;
  print(variabel);
}
